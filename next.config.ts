import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['media.istockphoto.com', 'github.com', 'hms-namycodes-organisations.s3.us-west-2.amazonaws.com']
  }
};

export default nextConfig;
